# Session encryption secret — use a long random string in production
SESSION_SECRET=change-me-to-a-random-string

# Admin password for /api/admin/* endpoints
ADMIN_PASSWORD=change-me-to-a-secret-password

# Node environment
NODE_ENV=production
